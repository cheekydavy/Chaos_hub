# Core Flask dependencies
Flask==3.0.3
Werkzeug==3.0.4  # Includes security (hashing) and utils (secure_filename)
Jinja2==3.1.4
MarkupSafe==2.1.5
itsdangerous==2.2.0
click==8.1.7

# Database support
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.35

# For Render's PostgreSQL (replaces SQLite in production)
psycopg2-binary==2.9.10  # PostgreSQL adapter

# Production server for Render
gunicorn==23.0.0

# Socket.IO for real-time chat
flask-socketio==5.3.6